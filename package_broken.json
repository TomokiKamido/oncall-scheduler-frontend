{
    "scripts": {
    "start": "NODE_OPTIONS='--openssl-legacy-provider' craco start",
    "start:no-reload": "FAST_REFRESH=false NODE_OPTIONS='--openssl-legacy-provider' craco start",
    "build": "craco build",
    "build:serve": "npm run build && npx serve -s build -l 3002",
    "preview": "npm run build && npx serve -s build -l 3002",
    "test": "craco test",
    "eject": "react-scripts eject",
    "firebase": "npx firebase --token $FIREBASE_TOKEN",
    "firebase:deploy": "npm run build && npx firebase deploy --token $FIREBASE_TOKEN",
    "firebase:serve": "npm run build && npx firebase serve --token $FIREBASE_TOKEN"
  },all-scheduler-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "NODE_OPTIONS='--openssl-legacy-provider' react-scripts start",
    "start:no-reload": "FAST_REFRESH=false NODE_OPTIONS='--openssl-legacy-provider' react-scripts start",
    "build": "react-scripts build",
    "build:serve": "npm run build && npx serve -s build -l 3002",
    "preview": "npm run build && npx serve -s build -l 3002",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "firebase": "npx firebase --token $FIREBASE_TOKEN",
    "firebase:deploy": "npm run build && npx firebase deploy --token $FIREBASE_TOKEN",
    "firebase:serve": "npm run build && npx firebase serve --token $FIREBASE_TOKEN"
  },
  "dependencies": {
    "@dnd-kit/core": "^6.3.1",
    "@dnd-kit/modifiers": "^9.0.0",
    "@dnd-kit/sortable": "^10.0.0",
    "@dnd-kit/utilities": "^3.2.2",
    "axios": "^0.27.2",
    "date-fns": "^2.29.3",
    "firebase": "^11.9.1",
    "firebase-admin": "^13.4.0",
    "lucide-react": "^0.263.1",
    "react": "^17.0.2",
    "react-dnd": "^16.0.1",
    "react-dnd-html5-backend": "^16.0.1",
    "react-dom": "^17.0.2",
    "react-router-dom": "^6.3.0",
    "react-scripts": "5.0.1",
    "typescript": "^4.9.5",
    "web-vitals": "^2.1.4"
  },
  "devDependencies": {
    "@craco/craco": "^7.1.0",
    "@types/node": "^16.18.34",
    "@types/react": "^17.0.2",
    "@types/react-dom": "^17.0.2",
    "firebase-tools": "^14.7.0",
    "serve": "^14.2.4"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "description": "このプロジェクトは、当直割作成ソフトのフロントエンド部分を実装したものです。Reactを使用して構築されており、スケジュール管理やスタッフ管理の機能を提供します。",
  "main": "index.js",
  "keywords": [],
  "author": "",
  "license": "ISC"
}
